# Don't upload node_modules — GitHub Actions installs fresh
node_modules/

# Don't upload build output — workflow regenerates it
# (but it IS committed by the workflow itself, so we keep tracking it)
# providers/

# OS junk
.DS_Store
Thumbs.db

# Editor junk
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
